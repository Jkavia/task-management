export const environment = {
  production: true,
  apiUrl: 'http://localhost:3000/api', // Should be replaced during build
  jwtSecret: 'production-jwt-secret-key' // Should be replaced during build
};